name: Parity Tests wasm32

on:
  push:
    branches: [main]
  pull_request:

jobs:
  parity-tests-wasm32:
    uses: ./.github/workflows/wasm32_test_base.yml
    with:
      test_name: Parity
      target: parity_tests
      test_command: node build/wasm32-release/tests/parity_tests.js
