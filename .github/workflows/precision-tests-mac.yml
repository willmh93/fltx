name: Precision Tests macOS

on:
  push:
    branches: [main]
  pull_request:

jobs:
  precision-tests-mac:
    uses: ./.github/workflows/precision_test_base.yml
    with:
      platform_name: macOS
      runner: macos-latest
      test_command: ./build/ninja-release/tests/precision_tests
